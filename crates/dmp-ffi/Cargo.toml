[package]
name = "dmp-ffi"
version.workspace = true
edition.workspace = true
description = "C FFI exports for dmp-core"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
dmp-core = { path = "../dmp-core" }
