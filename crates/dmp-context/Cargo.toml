[package]
name = "dmp-context"
version.workspace = true
edition.workspace = true
description = "Data model for DMP crash dump analysis"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
