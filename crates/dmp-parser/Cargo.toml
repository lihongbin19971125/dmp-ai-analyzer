[package]
name = "dmp-parser"
version.workspace = true
edition.workspace = true
description = "CDB output parsers and memory leak analyzer"

[dependencies]
dmp-context = { path = "../dmp-context" }
regex = "1"
