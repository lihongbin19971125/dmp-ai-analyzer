[package]
name = "dmp-py"
version.workspace = true
edition.workspace = true
description = "PyO3 Python bindings for dmp-core"

[lib]
crate-type = ["cdylib"]

[dependencies]
dmp-core = { path = "../dmp-core" }
pyo3 = { version = "0.23", features = ["extension-module"] }
